import styled from "styled-components";

const FormWrap = styled.div`
  Select ::placeholder {
    font-size: 10px;
    color: white;
  }
  label {
    margin-bottom: 1px;
  }
`;
export default FormWrap;
